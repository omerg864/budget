<!DOCTYPE html>
<html>
  <head>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        color: #333;
        line-height: 1.6;
      }
      .container {
        max-width: 600px;
        margin: 20px auto;
        background: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      .header {
        text-align: center;
        border-bottom: 1px solid #ddd;
        padding-bottom: 20px;
        margin-bottom: 20px;
      }
      .header h1 {
        color: #2c3e50;
      }
      .content {
        margin-bottom: 20px;
      }
      .button {
        display: inline-block;
        padding: 10px 20px;
        color: #fff;
        background-color: #007bff;
        border-radius: 5px;
        text-decoration: none;
        font-weight: bold;
      }
      .button:hover {
        background-color: #0056b3;
      }
      .footer {
        text-align: center;
        font-size: 12px;
        color: #aaa;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1><%= t('templates.shareLedger.title') %></h1>
      </div>
      <div class="content">
        <p>
          <%= t('templates.shareLedger.greeting') %>
          <strong><%= name %></strong>,
        </p>
        <p>
          <%= t('templates.shareLedger.body') %>
          <strong><%= ledgerName %></strong>
        </p>
        <p style="text-align: center">
          <a href="<%= link %>" class="button"
            ><%= t('templates.shareLedger.action') %></a
          >
        </p>
        <p><%= t('templates.shareLedger.ignore') %></p>
      </div>
      <div class="footer">
        <p>&copy; <%= new Date().getFullYear() %> Flow. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
