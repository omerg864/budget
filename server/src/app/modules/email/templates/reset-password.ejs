<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .button { display: inline-block; padding: 10px 20px; background-color: #007bff; color: white; text-decoration: none; border-radius: 5px; }
        .footer { margin-top: 20px; font-size: 0.8em; color: #666; }
    </style>
</head>
<body>
    <div class="container">
        <h2><%= t('templates.resetPassword.title') %></h2>
        <p><%= t('templates.resetPassword.greeting', { args: { name } }) %>,</p>
        <p><%= t('templates.resetPassword.message') %></p>
        <p>
            <a href="<%= link %>" class="button"><%= t('templates.resetPassword.button') %></a>
        </p>
        <p><%= t('templates.resetPassword.expiration') %></p>

        <div class="footer">
            <p><%= t('templates.resetPassword.footer') %></p>
        </div>
    </div>
</body>
</html>
